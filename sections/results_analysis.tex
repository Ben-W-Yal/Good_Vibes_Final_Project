\section{Results and Analysis}

\subsection{What we observe qualitatively}

In internal runs during development, GeoIntel reliably delivers four coupled outcomes end-to-end: \textbf{(i)} users can relocate quickly from headline-scale curiosity to map-scale geography; \textbf{(ii)} PDB sections tend to arrive as structured narratives with accompanying citations when Gemini~\cite{gemini-api} grounding succeeds; \textbf{(iii)} optional ion-backed terrain and OSM-style building meshes~\cite{cesium}, when enabled, give terrain-rich theaters recognizable vertical structure beyond satellite imagery alone; and \textbf{(iv)} per-event panels surface streamed analyst summaries plus conversational Q\&A tied to the selected record's context. PDF export remains available for slide-shaped classroom deliverables when a document artifact is preferable to screen capture.

Formal benchmarks were out of scope for this course delivery; claims below are qualitative unless noted.

\subsection{Evidence (figures)}

Figures~\ref{fig:globe}--\ref{fig:terrain} show the integrated dashboard~\cite{cesium}, PDB~\cite{gemini-api}, and terrain/buildings views. Figure~\ref{fig:event-analyst} highlights per-event Gemini output: auto-generated analyst summary and follow-up chat in the event detail sidebar.

\begin{figure}[ht]
  \centering
  \includegraphics[width=\linewidth]{figures/globe.png}
  \caption{Cesium globe with conflict/news-style markers and filtering chrome visible.}
  \label{fig:globe}
\end{figure}

\begin{figure}[ht]
  \centering
  \includegraphics[width=\linewidth]{figures/pdb.png}
  \caption{Presidential Daily Brief panel showing multi-section narrative output and citation chips.}
  \label{fig:pdb}
\end{figure}

\begin{figure}[ht]
  \centering
  \includegraphics[width=\linewidth]{figures/event-analyst.png}
  \caption{Event detail sidebar: streamed \textbf{AI analyst summary} of the selected item and \textbf{chat} for follow-up questions (same Gemini route, event-scoped context).}
  \label{fig:event-analyst}
\end{figure}

\subsection{Terrain and built environment}

The dashboard screenshot (Figure~\ref{fig:globe}) emphasizes breadth (filters, feeds, markers). Separately, enabling \textbf{Cesium ion global terrain} and \textbf{OSM-derived buildings}---controlled via client environment variables described in Implementation---changes how analysts read topography alongside structures: elevation cues and extruded footprints reduce ambiguity compared with imagery draped on an ellipsoid alone. Figure~\ref{fig:terrain} isolates that perspective-heavy view so reviewers can judge relief and vertical fidelity without confusing it with the PDB layout (which already resembles a printable deck).

\begin{figure}[ht]
  \centering
  \includegraphics[width=\linewidth]{figures/terrain.png}
  \caption{Perspective view with ion terrain and buildings enabled (coastal relief and extruded structures visible).}
  \label{fig:terrain}
\end{figure}

\subsection{Strengths}

\textbf{Integrated situational picture:} Combining feeds and trackers with terrain-aware navigation reduces context switching versus opening separate dashboards.

\textbf{Grounded summaries when healthy:} Gemini~\cite{gemini-api} outputs tied to citations improve inspectability versus unconstrained chat answers. Per-event chat inherits the same caution: answers are only as good as the normalized event fields and related URLs supplied in context.

\textbf{Explicit operational boundaries:} Section-by-section generation surfaces failures locally instead of collapsing an entire deck silently.

\subsection{Limitations and brittle behavior}

\textbf{Upstream variability:} GDELT-like endpoints~\cite{gdelt} can throttle or return sparse windows; AIS feeds depend on reception patterns and credential tiers.

\textbf{Model behavior:} Grounded briefings occasionally truncate JSON-shaped outputs under token limits or overload responses; citation counts fluctuate by topic.

\textbf{Imagery extraction:} Article imagery depends on publisher markup (Open Graph/Twitter tags); many pages yield no embeddable preview image even when reporting is solid.

\textbf{Geolocation realism:} Automated coordinates can default to coarse regions when upstream feeds omit precise lat/lon.

\subsection{Informal evaluation}

We exercised representative workflows during integration testing (single-machine demos): globe navigation, feed toggling, per-event summaries and chat, PDB generation under configured Gemini~\cite{gemini-api} keys, and PDF export.
No structured human-subjects study was conducted; informal consensus among teammates emphasized speed-to-context as the strongest perceived gain versus bookmark tabs alone.

\subsection{Operating economics (order-of-magnitude)}

Costs split into three buckets:

\begin{enumerate}[leftmargin=*]
  \item \textbf{Gemini usage:} PDB generation triggers multiple grounded completions per full brief~\cite{gemini-api}; expense scales with sections generated and repeat trials during demos.
  \item \textbf{Third-party trackers/maps:} Optional licensed trackers (where configured) carry vendor-specific metering; Cesium ion terrain/buildings~\cite{cesium} depends on ion quotas tied to project tokens.
  \item \textbf{Hosting footprint:} Local development requires modest CPU/RAM; production deployment cost depends on hosting provider selection not exercised here.
\end{enumerate}

\subsection{Competitive landscape}

\textbf{News-only aggregators} optimize headlines but rarely fuse spatial trackers with citation-grounded narratives.

\textbf{Standalone globe tools} visualize geography without coherent briefing pipelines tuned to structured intelligence sections.

\textbf{Consumer chatbots} answer questions conversationally yet omit reproducible APIs under controlled adapters and persistent globe-linked inspection patterns implemented here.

GeoIntel differentiates through \textbf{(i)} adapter-backed ingestion under explicit caching discipline and \textbf{(ii)} coupling Gemini-grounded PDB outputs~\cite{gemini-api} with terrain-centric exploration~\cite{cesium} rather than chat-only summaries.
